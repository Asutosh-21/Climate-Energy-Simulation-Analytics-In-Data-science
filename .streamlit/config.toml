[theme]
base = "light"
primaryColor = "#0b6efd"
backgroundColor = "#f7f9fc"
secondaryBackgroundColor = "#ffffff"
textColor = "#111827"
font = "sans serif"

[server]
headless = true
# Note: don't set enableCORS=false together with enableXsrfProtection=true (Streamlit will override it)
# When deploying to Streamlit Cloud, do not set a fixed port here — the platform manages ports.
